<ul>
	<% @posts.each do |post| %>
		<li>
			<a href="/posts/<%= post.id %>"><%= post.title %></a> | <%= link_to 'edit', edit_post_path(post) %> | <%= link_to 'destroy', post_path(post),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
		</li>
	<% end %>
</ul>